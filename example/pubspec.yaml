name: example_timeline_tile_plus
description: A package to help you build highly customisable timelines with Flutter.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_highlighting: ^0.9.0+11.8.0
  timeline_tile_plus:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
